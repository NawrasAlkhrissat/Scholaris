<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>School Profile | Details</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="/css/school.css">
</head>

<body>

    <%- include('./partials/nav.ejs') %>

        <div class="dashboard-container">

            <aside class="sidebar">
                <div class="sidebar-title">Main Menu</div>
                <a href="/school/user/<%= userId %>" class="sidebar-link"><span>👤</span><span>My
                        Profile</span></a>
                <a href="/school" class="sidebar-link active"><span>🏫</span>My School</a>
                <a href="/school/subject" class="sidebar-link"><span>📚</span><span>
                        <%= role=='admin' ? 'All Subjects' : 'My Subjects' %>
                    </span></a>
                <a href="/school/academic-calendar" class="sidebar-link"><span>📅</span><span>Calendar</span></a>
            </aside>

            <main class="main-content">


                <div class="school-hero">

                    <div class="hero-content">
                        <h1 class="school-name">
                            <%= school.name %>
                        </h1>
                        <div class="school-manager">
                            <span>Managed by:</span>
                            <span class="manager-badge">
                                👨‍💼 <%= school.managerName %>
                            </span>
                        </div>
                    </div>
                    <% if(role==="admin" ){ %>
                        <a href="/school/editSchool/<%= school._id %>" class="btn-edit-hero">
                            <span>✏️</span> Edit Profile
                        </a>
                        <a href="/school/dashboard" class="btn-edit-hero">
                            <span>📊</span> Dashboard
                        </a>
                        <% } %>

                </div>

                <div class="info-grid">

                    <div class="info-card">
                        <div class="icon-box icon-location">📍</div>
                        <div class="info-text">
                            <h4>Address</h4>
                            <p>
                                <%= school.address %>
                            </p>
                        </div>
                    </div>

                    <div class="info-card">
                        <div class="icon-box icon-phone">📞</div>
                        <div class="info-text">
                            <h4>Phone Number</h4>
                            <p>
                                <%= school.contacts.phone %>
                            </p>
                        </div>
                    </div>

                    <div class="info-card">
                        <div class="icon-box icon-email">✉️</div>
                        <div class="info-text">
                            <h4>Email Address</h4>
                            <p>
                                <%= school.contacts.email %>
                            </p>
                        </div>
                    </div>

                </div>

                <div class="gallery-section">
                    <h2>Campus Gallery</h2>

                    <% if(school.photos && school.photos.length> 0) { %>
                        <div class="gallery-masonry">
                            <% school.photos.forEach((photoUrl, index)=> { %>
                                <div class="gallery-item">
                                    <img src="<%= photoUrl %>" alt="School Photo <%= index + 1 %>">
                                    <div class="gallery-overlay">
                                        <span class="overlay-text"></span>
                                    </div>
                                </div>
                                <% }) %>
                        </div>
                        <% } else { %>
                            <div
                                style="text-align: center; color: #999; padding: 40px; background: white; border-radius: 16px;">
                                <p>No photos available for this school yet.</p>
                            </div>
                            <% } %>
                </div>
            </main>
        </div>
        <footer>
            <p>All rights reserved &copy; 2025 - SMS School Management System</p>
        </footer>
        <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
        <% if(action && action.includes('updatedSchool')){ %>
            <script>
                Swal.fire({
                    icon: "success",
                    title: "Success",
                    text: "School updated successfully.",
                });
            </script>
            <% } %>
</body>

</html>